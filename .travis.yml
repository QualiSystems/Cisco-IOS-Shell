language: python
python:
- '2.7'

before_install:
- pip install -r requirements.txt --allow-external
- pip install -r test_requirements.txt --allow-external

install:
    - python setup.py install

script: python setup.py sdist --format zip